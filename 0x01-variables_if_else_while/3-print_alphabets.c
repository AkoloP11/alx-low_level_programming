#include <stdio.h>
/**
 * main -entry point
 *
 *
 * Return: Always 0(Success)
 */
int main(void)
{
	int b;

	for (b = 97; b <= 122; b++)
	{
		putchar(b);
	}
	for (b = 65; b <= 90; b++)
	{
		putchar(b);
	}
		putchar('\n');

		return (0);
}
